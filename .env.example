# LLM key for recipe generation (Google AI Studio). Without it, the app
# serves a built-in mock recipe so the UI remains fully testable.
GOOGLE_GENERATIVE_AI_API_KEY=

# Proxy guardrails (ADR-0002)
GENERATIONS_PER_10MIN_PER_IP=10
DAILY_GENERATION_CAP=100
